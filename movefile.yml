global:
  sql_adapter: default

local:
  vhost: https://demo.dev
  wordpress_path: /Users/TheMatrix/Sites/valet/demo/web/wp # use an absolute path here

  database:
    name: wp_demo
    user: root
    password: "" # could be blank, so always use quotes around
    host: 127.0.0.1

production:
  vhost: https://ruggedchuck.com
  wordpress_path: /srv/users/serverpilot/apps/demo/public/current/web/wp # use an absolute path here

  database:
    name: wp_demo
    user: 1445c64a2834
    password: darswright
    host: localhost
    # port: 3308 # Use just in case you have exotic server config
    # mysqldump_options: --max_allowed_packet=1G # Only available if using SSH

  exclude:
    - '.git/'
    - '.gitignore'
    - '.sass-cache/'
    - 'node_modules/'
    - 'bin/'
    - 'tmp/*'
    - 'Gemfile*'
    - 'Movefile'
    - 'movefile'
    - 'movefile.yml'
    - 'movefile.yaml'
    - 'wp-config.php'
    - 'wp-content/*.sql.gz'
    - '*.orig'

#  paths: # you can customize wordpress internal paths
#    wp_content: web/app/wp-content
#    uploads: web/app/uploads
#    plugins: web/app/plugins
#    mu_plugins: web/app/mu-plugins
#    themes: web/app/themes
#    languages: web/app/languages

  ssh:
    host: 128.199.179.122
    user: serverpilot
    password: TheMatrix1 # password is optional, will use public keys if available.
  #   port: 22 # Port is optional
  #   rsync_options: --verbose --itemize-changes# Additional rsync options, optional
  #   gateway: # Gateway is optional
  #     host: host
  #     user: user
  #     password: password # password is optional, will use public keys if available.

  # ftp:
  #   user: user
  #   password: password
  #   host: host
  #   passive: true
  #   scheme: ftps # default ftp

  # hooks: # Remote hooks won't work with FTP
  #   push:
  #     before:
  #       local:
  #         - 'echo "Do something locally before push"'
  #       remote:
  #         - 'echo "Do something remotely before push"'
  #     after:
  #       local:
  #         - 'echo "Do something locally after push"'
  #       remote:
  #         - 'echo "Do something remotely after push"'
  #   pull:
  #     before:
  #       local:
  #         - 'echo "Do something locally before pull"'
  #       remote:
  #         - 'echo "Do something remotely before pull"'
  #     after:
  #       local:
  #         - 'echo "Do something locally after pull"'
  #       remote:
  #         - 'echo "Do something remotely after pull"'

# staging: # multiple environments can be specified
#   [...]
